{
  "name": "cgm-remote-monitor",
  "description": "Nightscout Web Monitor (cgm-remote-monitor) - Heroku deploy configuration",
  "repository": "https://github.com/nightscoutbarni/cgm-remote-monitor",
  "stack": "heroku-20",
  "buildpacks": [
    { "url": "heroku/nodejs" }
  ],
  "env": {
    "API_SECRET": {
      "description": "A secret passphrase at least 12 characters long",
      "required": true
    },
    "MONGODB_URI": {
      "description": "MongoDB connection string",
      "required": true
    },
    "MONGODB_COLLECTION": {
      "description": "Mongo collection for CGM entries",
      "value": "entries"
    },
    "DISPLAY_UNITS": {
      "description": "Display units for glucose values (mg/dl or mmol/L)",
      "value": "mg/dl"
    }
  },
  "scripts": {
    "start": "node lib/server/server.js"
  }
}