# Real-browser regression tests

This is the first migration in the [jsdom retirement plan](../plans/jsdom-retirement.md). It uses the existing Mocha runner with pinned development-only `playwright-core` 1.63.0. Browser downloads are explicit; ordinary `npm ci`, production installs and Docker builds do not download browsers.

```sh
npm ci
npx --no-install playwright-core install chromium firefox webkit
npm run test:browser
NIGHTSCOUT_TEST_BROWSER=firefox npm run test:browser
NIGHTSCOUT_TEST_BROWSER=webkit npm run test:browser
```

On Linux, install browser system dependencies with `npx --no-install playwright-core install --with-deps chromium firefox webkit`. A missing package, browser executable or emitted application bundle fails the suite. CI runs all three engines on both exact supported Node floors. The Node/MongoDB 5/6 matrix remains unchanged, and Docker validation/publication depends on the browser job as well as backend tests. Modern Playwright WebKit is not proof of compatibility with every older iOS version listed in `.browserslistrc`; historical-browser transformation and release smoke gates still apply.

## Coverage mapping

All six browser jobs passed the comparison phase on both exact Node floors. The old bundle smoke file and the four served-browser jsdom cases are now replaced; Node parser/client coverage remains.

| Existing assertion | Real-browser replacement |
| --- | --- |
| `bundle.smoke.test.js`: namespace object, client object/init, reportclient/profileclient functions, units object/conversion functions | `tests/browser/bundle.test.js` loads the actual emitted production bundle over HTTP and checks all eight types. Grouping five Mocha cases into one does not remove assertions. This is structural coverage, not report-calculation or full-page rendering coverage. |
| `dependency-socket-parser.test.js`: served browser polling, WebSocket and polling-to-WebSocket upgrade over initial connection plus two reconnects | `tests/browser/socket-client.test.js` loads `/socket.io/socket.io.js` from the installed real server and preserves transport, snapshot, Unicode, large message and binary acknowledgement assertions for all three cycles. |
| Served browser automatic reconnection after two network drops | Real browser closes the engine twice, verifies reconnection/data and asserts one server peer after each cycle. Node-client, malformed-packet and parser coverage stays in the Node suite. |
| Harness guarantees | `tests/browser/isolation.test.js` exercises fetch, XHR, script/image/style/iframe loads, worker fetch, redirect and WebSocket attempts against an unapproved local server over two contexts. It asserts zero requests/handshakes, empty initial storage, blocked service workers and closure of all pages after deliberate failures. |

## Fixture behavior

Each test gets a fresh non-persistent context and closes it in `finally`; root hooks detect/clean any leaked contexts and close the browser at suite completion. Uncaught page errors and unexpected blocked requests fail the helper. Only the exact `http://127.0.0.1:<fixture-port>` origin is permitted, not arbitrary loopback hosts or ports.

HTTP routing fetches one finite response with `maxRedirects: 0` and fulfills its actual headers/body. Redirect responses are rejected, including same-origin redirects: these fixtures do not test redirect navigation or streaming HTTP responses. A first implementation using `route.continue()` failed the redirect isolation regression because Chromium followed the redirect without re-entering the handler. Allowed WebSockets retain the native browser connection; only disallowed destinations are intercepted and closed. Chromium receives local-network permission scoped to the fixture origin because fulfilled documents lack a network address; the HTTP/WebSocket allow-list remains enforced. Service workers are blocked explicitly. These test-fixture controls must be revisited when adding new transport/navigation requirements.

The [Playwright library API](https://playwright.dev/docs/library) supports reuse of Mocha rather than adding another general test runner. [Context routing and permissions](https://playwright.dev/docs/api/class-browsercontext) provide the isolation hooks. The application does not acquire a browser-automation runtime dependency or a DOM substitute.

## Current evidence and next gate

Initial local Node 22.23.2 validation passes eight cases in Chromium 153.0.8010.12 and WebKit 26.6. Firefox 1543 fails to launch its temporary profile on this macOS host, including with a fresh `/tmp` profile location. Hosted Linux Chromium, Firefox and WebKit all passed on Node 22.23.2 and 24.20.0 in [comparison CI](https://github.com/nightscout/cgm-remote-monitor/actions/runs/33984607458). The first isolation test exposed and then verified the redirect fix. All retained lock entries remain identical to parent `d87f77e6`; only the development package path playwright-core is added. Production dependency entries remain unchanged. The refreshed main suite passed 2,004 tests (three existing pending), client-core 283 and dependency compatibility 313; the nine removed Node cases are covered by the new browser suite. Chromium/WebKit also passed on the Node 24.20.0 floor. Both builds, changed-file lint and actionlint passed, and production bundles are byte-identical (app 1,761,400 bytes; clock 152,131 bytes). No test-resource saving is claimed; see the measured costs below.

The comparison retained the old tests until the replacement jobs passed and this assertion mapping was checked. The namespace test also fails when `reportclient` is deliberately replaced with `undefined`; the mutation was isolated from the production bundle. Full jsdom retirement requires migrating the other consumers listed in the plan; this first slice does not remove the package yet.

## Measured test-tool cost

Seven fresh matched processes per runner used Node 22.23.2, parent `d87f77e6` and initial browser head `87663161`, with identical production bundles. That comparison imported the Playwright wrapper, which only re-exports the same core API; the final code imports playwright-core directly. The timing/RSS figures remain labeled as initial-build measurements, not measurements of the final import cleanup. The selected old suite has nine cases (five bundle plus four served-browser); the replacement has five grouped cases preserving those assertions. New isolation tests are excluded from this comparison. Each browser process is closed between samples. Median [min–max]:

| Selected suite | Wall seconds | Peak summed process-tree RSS bytes |
| --- | ---: | ---: |
| jsdom | 0.903 [0.884–1.335] | 172,883,968 [168,460,288–175,570,944] |
| Chromium | 1.139 [1.121–1.470] | 486,883,328 [486,440,960–488,767,488] |
| WebKit | 1.890 [1.853–2.302] | 328,761,344 [328,187,904–331,218,944] |

Reproduce with built parent/candidate worktrees and browsers installed for the candidate:

```sh
python3 tools/audits/browser-test-probes.py /path/to/parent /path/to/candidate /path/to/results --node /path/to/node --browsers chromium webkit
```

The Unix probe samples `ps` about every 25 ms and sums the Mocha process and current descendants. Shared pages may be counted more than once; this is neither unique physical memory nor server heap, and short peaks can be missed. It includes browser launch and monitoring overhead. Real browsers cost more test memory here; their value is replacing a DOM emulator with actual browser behavior, not reducing this fixture's RAM. Firefox has no local cost result because launch failed; Linux CI passed its functional tests.

The final dependency is `playwright-core` directly: the `playwright` entry point only re-exports it, so the wrapper/test-runner package is unnecessary for Mocha. This avoids 5,094,512 bytes and one package path compared with the initial comparison build. Added npm file contents are 13,453,369 bytes for playwright-core. The explicit macOS browser installation additionally occupies 1,183,917,464 bytes of regular-file contents: Chromium 375,624,473; headless shell 204,662,008; FFmpeg 2,606,838; Firefox 304,282,055; WebKit 296,742,090. These are uncompressed installed contents, not network downloads, unique disk blocks or Linux image bytes. The existing shared FFmpeg cache is counted in that total even though it need not be downloaded again. The final jsdom removal must report net npm savings separately from browser-tool storage; this first migration temporarily adds one dev path. Production package entries and emitted bundles are unchanged, so no production-image or server-memory saving is claimed.

Rollback the complete browser-migration child PR, restoring its old tests and CI wiring together. No application, database or browser-storage migration is involved.

## DOMPurify reference migration

`tests/browser/dompurify.test.js` replaces all 15 cases from `tests/dependency-dompurify.test.js` with the installed DOMPurify browser distribution and a real DOM. DOMPurify remains an independent development-only comparator; production still uses sanitize-html.

The eight attack categories retain two sanitizer calls each, recursive inspection of nested `template.content`, dangerous tag/handler/URL checks and the ampersand sentinel. Four visible-note cases, exact safe/nested-template serialization and two SVG presentation-attribute cases remain. Every case gets its own isolated context. The replacement passes all 15 cases in Chromium and WebKit locally; bypassing sanitization in a temporary mutation makes the event-handler test fail with `onerror`, demonstrating the assertion still detects the bug. All three engines in current integration CI remain required before merge. The larger differential corpus and production sanitizer mutation tests are mapped in the following migration section.

This step changes no dependency versions, application behavior, output policy, database or browser assets. Rollback restores the old test file and removes its mapped browser file together. It does not remove jsdom yet.

Against browser-foundation parent `eb01da5b`, the full local browser suite passes 23 cases in Chromium and WebKit on both Node 22.23.2 and 24.20.0. The Node suite passes 1,989 cases with the same three existing pending tests (2,004 minus the 15 migrated cases); client-core passes 283 and dependency compatibility passes 298 (313 minus 15). A clean install/production build and changed-file lint pass. Firefox validation is provided by the required Linux CI jobs because its downloaded binary cannot launch a profile on this Mac. This is a coverage migration, with no additional package or production-memory claim.

## Sanitizer corpus and production reparsing migration

`tests/browser/sanitizer-differential.test.js` replaces the 300 cases in the former top-level differential suite and the one browser-parsing case in `tests/security.test.js`. All 30 corpus objects are unchanged, and all 300 original case titles remain, beneath a new browser-suite parent. The mapping is 120 sanitizer-output safety cases (30 inputs × four sanitizers), 30 divergence records, 120 cases using the existing EJS escaping model, 30 raw `textContent` cases, plus the production case with 12 hostile payloads and three parse/serialize rounds each. Server sanitizer and storage contracts remain in the Node security suite.

Production, xss and strict sanitize-html outputs are still computed with their actual Node modules; DOMPurify runs its installed browser distribution. Each parser case gets a fresh isolated context. `DOMParser` parses an inert full HTML document, preserving the old full-document parser check without executing attack markup in the fixture page. This checks parsed structure, not script execution, layout or a complete application rendering flow. The EJS helper remains the same escaping model as before; actual output-sink and rendering integration tests are separate migration work. The diagnostic divergence report tolerates incomplete matrices during focused or failed runs without suppressing case failures.

On the initial candidate, all 301 cases pass in Chromium and WebKit on Node 22.23.2. The 30 DOMPurify outputs match the previous jsdom results exactly in both engines, with zero attempted network requests during the isolated output comparison. A temporary production-sanitizer bypass fails 11 corpus assertions for script tags, handlers and unsafe URLs; bypassing the sanitizer in the repeated-parsing case also fails. The existing xss less-than-note truncation remains visible in the divergence record; xss is a test comparator, not a proposed runtime replacement.

The remaining main suite passes 1,688 cases with three existing pending tests (1,989 minus 301); no tests were disabled. NYC still reports the same covered line, branch and function counts, with one fewer covered statement in that Node job as these cases move to a separate browser job. Browser coverage is not included in NYC. No application code, dependency version, stored-data policy or browser assets change. Current-parent Chromium/Firefox/WebKit CI on both Node floors, backend checks and native Docker validation remain merge gates. Rollback restores both former test locations and removes the mapped browser file together.

The full browser suite also passes all 324 cases in Chromium and WebKit on Node 24.20.0; client-core passes 283 and dependency compatibility 298. The diagnostic matrix covers all 30 inputs with zero incomplete rows and records the same 15 divergent inputs. All three browser engines in hosted CI remain required for this child.

## Clock client migration

`tests/browser/clock-client.test.js` replaces all five cases in the former `tests/clock-client.test.js` using the emitted production clock bundle. It preserves exact component order and inline size assertions, hostile face-configuration rejection, both directions of browser/server mg/dL versus mmol preference and matching-unit server-scaled values. Each case now completes two actual same-origin AJAX requests and render cycles in a fresh browser context, retaining the prior assertions after both cycles. The fixture verifies the exact `/api/v2/properties` GET URL and response; it uses the browser's real localStorage and jQuery AJAX rather than a Node global stub or direct callback.

The five cases pass in Chromium and WebKit on both Node 22.23.2 and 24.20.0. All five original case titles are retained. A temporary mutation that disables unit mismatch handling fails both conversion cases. Clean install/production build and changed-file lint pass. Current-parent main/core/dependency and full browser CI remain required before merge. No clock implementation, display policy, stored preferences or dependencies change; the test does not claim complete clock initialization, authentication or historical iOS coverage. Rollback restores the old file and removes the mapped browser file together.

Against corpus parent `75f1d959`, the remaining main suite passes 1,683 tests (1,688 minus five), with the same three pending cases; client-core passes 283 and dependency compatibility 298. The full Chromium browser suite passes 329 cases. NYC no longer includes the clock module loaded only by those former Node tests, so its changed coverage percentages are not a coverage improvement; the clock behavior is now exercised by the separate browser job. Browser coverage is not collected by NYC.

A follow-up authentication migration exposed missing UTF-8 headers in these raw HTTP test fixtures. Express HTML/static responses already declare UTF-8; the clock fixture now does too, and its component case checks the actual unicorn emoji as well as numeric values. The charset assertion and emoji check fail with the former fixture headers. This fixes test fidelity without changing application behavior. Current-head CI is rerun after this correction.

Local WebKit 26.6 still reported windows-1252 for a fulfilled HTML response with a UTF-8 header, so the clock fixture also declares UTF-8 in its HTML meta element. This keeps the intended document/script encoding explicit across the routed browser engines. The assertion is retained, not relaxed.

An isolated WebKit probe confirmed the boundary: native HTTP retains `text/html; charset=utf-8` and renders the emoji correctly; the fulfilled response exposes `text/html` and defaults to windows-1252; adding the UTF-8 meta element restores the native text/encoding result. The probe uses the same pinned browser package, so the fixture workaround does not assert that routed response headers are identical on WebKit.

## Authentication migration and fixture encoding

`tests/browser/hashauth.test.js` replaces all eight cases in `tests/hashauth.modern.test.js`. It uses the authentication module exposed by the actual production bundle, real jQuery, native localStorage and a local HTTP verification response. The preserved cases cover unauthorized/authorized text, raw and entity-encoded subject names, saved versus session-only hashes, removal over two cycles, the short-secret alert and the JavaScript hashing fallback when both SubtleCrypto and TextEncoder are unavailable. The standard path also asserts that native Web Crypto performs the hash; both paths retain the same known SHA-1 vector. The fixture records the actual verification GET, cache-buster and API-secret hash header. It stubs translation and reload callbacks, not the DOM or authentication implementation; it does not test the complete application initialization or real server permission policy.

One additional regression saves authentication, reloads the page and production bundle twice, then verifies that the stored hash restores authentication without rehashing. Removal still clears the hash and native storage afterward. A temporary storage bypass fails the saved-hash assertion, and rendering an unescaped subject fails the image-element assertion. All eight old case titles remain; the reload case is additional coverage.

The migration exposed a fixture encoding error: raw HTTP responses lacked UTF-8, which corrupted the bundled entity decoder's Unicode data and made the encoded-subject case fail. Express HTML/static responses already declare UTF-8. The browser fixture servers now send UTF-8 headers and include an HTML meta declaration, as needed for WebKit's fulfilled-response behavior documented above. The assertion is preserved, and production source is unchanged. These fixtures check rendered content; they do not establish byte-for-byte native HTTP header behavior for every engine.

The initial eight-case migration passes in Chromium and WebKit on Node 22.23.2 and 24.20.0, with full 337-case Chromium/WebKit suites on Node 22. The final nine-case authentication suite passes in Chromium and WebKit on both Node floors, with full 338-case Chromium Node 22 and WebKit Node 24 runs. All required current-parent hosted checks remain a merge gate. The remaining main suite passes 1,675 cases with three existing pending tests (1,683 minus eight); client-core passes 283 and dependency compatibility 298. NYC reports lower coverage in that Node job because browser execution is not instrumented there; this is an instrumentation-scope change, not deletion of the mapped assertions. No dependency, production policy, stored-data format or application bundle changes. Rollback restores the old authentication test and removes its browser replacement; preserve the independently valid fixture UTF-8 correction unless reverting that correction too.

## Admin actions and date-range migration

`tests/browser/admin-actions.test.js` replaces all seven cases in `tests/admintools.modern.test.js` and three in `tests/daterangedelete.test.js`. The actual emitted application bundle supplies the admin plugins. The fixture preserves the component boundary of the old modern tests, supplying real action containers and minimal translation/authentication/timezone interfaces; it does not claim full admin-page initialization or backend permission coverage. Native jQuery requests reach an isolated local HTTP server with fixture responses. No real database deletion occurs.

Preserved contracts cover cleanup status/button text, day inputs, future entries/treatments, literal hostile stored fields, all-collection date-range requests, timezone-aware epoch/ISO bounds, record totals, one confirmation, completion callbacks and invalid-collection rejection. Three additional cases assert exact UTC boundaries for New York's 23-hour and 25-hour daylight-saving dates and zero HTTP requests after dismissing the native deletion confirmation. Expectations use fixed UTC values rather than recomputing them with the implementation's Moment algorithm. The fixture waits for jQuery AJAX completion as well as the plugin callback.

All ten former case titles are retained under the new browser parent. The 13 cases pass in Chromium and WebKit on Node 22.23.2 and 24.20.0, with full 351-case Chromium Node 22 and WebKit Node 24 suites. Temporary mutations that force UTC or ignore a cancelled confirmation each fail the relevant regression. Clean install/production build and changed-file lint pass. Current-parent hosted browser, backend, npm 12, CodeQL and native Docker checks remain merge gates.

Deleting the old Node fixtures exposed a deterministic jQuery setup ordering bug in the remaining D3 tests: a foreign global window makes jQuery export an already-bound function instead of its factory. `installDomGlobals` now hides both window/document during factory loading and restores their descriptors in `finally`. Two focused tests verify fresh-window ownership and exception restoration over two cycles, and both fail against the old helper. This is a transitional test-harness repair, not a production change. Rollback restores the two former test files and removes their browser replacement together; retain the independently valid fixture repair unless reverting that repair too.

With that repair, the full Node suite passes 1,667 cases with three existing pending tests (1,675 minus ten migrated cases plus two helper regressions); client-core passes 283 and dependency compatibility 300. The initial run failed in the D3 setup before the deterministic helper repair; it was not an unchanged retry. The combined jsdom/D3 suite also passes 38 cases on Node 24.20.0. NYC's Node job now measures fewer browser-loaded modules; browser execution is not included in its coverage report, and changed percentages are not claimed as an improvement. No application, manifest, lockfile or browser asset changes are made.

## Report loading, Daily Stats and reconnect migration

`tests/browser/report-sgv-pipeline.test.js` replaces all eight cases from `tests/report-sgv-pipeline.test.js` and both cases from `tests/report-reconnect.test.js`. Every original case title remains. The production application bundle supplies the report client, registry, Daily Stats renderer, jQuery and Flot. `tests/browser/modules.source.js` exposes the actual private `lib/utils` factory for component tests; `modules.js` compiles it once per process using the existing webpack configuration into `node_modules/.cache/nightscout-browser-tests`, separately from application assets. Compiler errors fail the tests and the compiler closes on success/failure. No new package or production export is introduced.

The SGV fixture preserves real loading/filtering, glucose conversion and table rendering while serving finite loopback HTTP responses. Its jQuery JSON converter freezes parsed CGM records as the old fixture did. The plot observer retains Flot metadata and invokes real canvas drawing; statistics selectors inspect only the report table's own rows, excluding newly present nested legend tables. Both mg/dL and mmol cases preserve dense/five-minute filtering, band counts, pie inputs, A1c before display rounding, independent daily totals across an empty day, cached data and identical re-rendered results. The actual report registry is limited to Daily Stats rendering for these cases; this is component coverage, not full application authorization or every report plugin.

The reconnect cases now use the actual registry/HTML and native food request, retaining the single Show handler, user-selected date/tab, placeholder/style counts and unique style IDs. Two reconnect/reinitialization cycles preserve state and do not fetch food again. A separate repeated HTML setup check keeps the selected placeholder visible. The test invokes the authorization callback as before; transport reconnection is covered by the separate Socket.IO suite.

Isolated production-bundle mutations prove sensitivity: removing the GUI guard yields two click handlers, repeats HTML setup and resets the date; disabling historical data reuse yields six rather than three CGM requests in both unit modes. Neither mutation changes the committed source or production assets. The initial browser attempt exposed test-observer issues (missing Flot metadata, then nested legend rows); preserving the metadata and narrowing the selector resolves them without changing application behavior or relaxing statistics assertions.

The ten mapped cases pass in Chromium/WebKit on both Node 22.23.2 and 24.20.0; full Chromium Node 22 and WebKit Node 24 suites pass 361 cases. The main suite passes 1,657 cases with three existing pending tests (1,667 minus ten migrated cases), client-core passes 283 and dependency compatibility 300. The helper bundle is 47,185 bytes locally, generated only for browser tests; application/clock bundle hashes match the parent. All required current-parent hosted checks remain merge gates. NYC does not collect browser execution, so changes in its Node coverage totals are not claimed as coverage improvements. Rollback restores the two old files and removes their mapped browser suite and unused test-only entry/helper together. jsdom and its remaining consumers are still present.

## Settings and profile-output migration

`tests/browser/profile-settings.test.js` replaces all six `tests/profile-sinks.test.js` cases and the one `tests/browser-settings.test.js` case. All original titles/assertions remain: care portal, bolus calculator, treatment/profile editor option values, literal profile-report/Loopalyzer text, and numeric descending bolus thresholds. The existing application bundle supplies report/profile editors and jQuery UI. The separate test-only entry adds actual browser-settings, careportal and boluscalc factories; no production export, package or implementation changes are made.

The options preserve distinct raw keys `A&B` and `A&amp;B` even though both display as `A&B`, and hostile names remain literal text. Native selection checks repeat all three names twice in care portal, bolus calculator and profile editor. Extending the old minimal profile fixture initially failed because its missing profile-name input made a change look like a rename; the replacement now serves the actual form from `views/profileindex.html`. Its real jQuery HTTP request loads a finite local profile response. The treatment editor opens the actual jQuery UI dialog instead of a dialog stub. Known fixture script elements are excluded by identity from global injection checks, so additional script elements or handlers still fail.

The component client interfaces and bolus-calculation stubs preserve the old test boundaries. These cases validate output and option wiring, not complete authorization, treatment submission, dosing calculations or persistence to MongoDB. The profile editor checks its initial GET and native switching without submitting a write. Isolated mutations of the actual private modules prove sensitivity: decoding option values collapses two distinct keys and fails; reversing numeric ordering also fails. No mutation is committed.

Clean install/production build and changed-file lint pass. Seven mapped cases pass in Chromium/WebKit on Node 22.23.2 and 24.20.0; full Chromium Node 22 and WebKit Node 24 suites pass 368 cases. Main passes 1,650 with the same three pending tests (1,657 minus seven migrated cases), client-core 283 and dependency compatibility 300. Production app/clock hashes match the parent. The expanded test-only helper is 99,701 bytes locally; it changes test tooling, not server memory or production assets. NYC excludes browser execution, so its changed percentages are not a coverage gain. All current-parent hosted checks remain required before merge. Rollback restores the two old files, removes the mapped browser suite and drops its now-unused test-entry exports together.

## Chart interaction migration

`tests/browser/chart-interactions.test.js` replaces all 22 DOM cases in `tests/dependency-d3.test.js`; its two pure color/interpolation/security cases remain in Node. Every original case title and assertion is retained. The existing test-only webpack entry adds a deterministic chart factory using the actual chart and renderer. D3 and jQuery come from the production application bundle. The old `fixtures/d3-chart.js` has no remaining consumer and is removed.

The fixture loads production `main.css`, omitting only its external Google font import for network isolation. Native SVG geometry replaces jsdom bounding-box and brush-extent overrides. Missing CSS initially exposed the browser's default 300-pixel SVG width; loading the real stylesheet restores the expected 900-pixel chart. Brush mouse gestures use actual browser hit-testing. Touch uses Playwright's native DOM event construction with a touch-enabled context; WebKit requires `document.createTouch` internally and can report zero `maxTouchPoints` despite installed touch listeners. The fixture asserts the actual D3 touch listener instead. Dispatched touch events exercise handler behavior, not physical-device gesture arbitration. Other treatment/hover mouse events preserve the prior directly targeted event contract.

Preserved checks cover glucose/forecast positions and hover content in both units, tooltip placement/clamping, repeated brush centering and drag, disabled editing, cancelled touch movement, duration width, literal profile text and all six treatment operations. Native confirmations are accepted/dismissed over two cycles, checking cleanup and each cycle's captured socket payload. The application services and socket boundary remain deterministic fixtures; these cases do not submit real treatments or establish complete application initialization, backend authorization or dosing correctness.

Isolated builds of deliberately broken source prove regression sensitivity: removing brush clamping produces an out-of-range selection in both mouse/touch cases; ignoring a dismissed removal confirmation emits two forbidden `dbRemove` operations and fails. These mutations do not change committed source or production assets.

Clean install/production build and changed-file lint pass. The main suite passes 1,628 cases with three existing pending tests (1,650 minus 22 migrated cases), client-core 283 and dependency compatibility 278 (300 minus 22). The 22 mapped cases pass in Chromium/WebKit on both Node floors; full Chromium Node 22.23.2 and WebKit Node 24.20.0 suites pass 390 cases. Current-parent hosted checks remain merge gates, including Firefox on Linux because its local binary cannot launch on this Mac. Production app/clock hashes match the parent; the expanded private test helper is 233,739 bytes locally. No dependency or production-memory saving is claimed for this slice. Browser execution is outside NYC's Node coverage report. Rollback restores the old DOM cases/helper and removes their browser replacements together; remove the touch fixture option only when no consumer remains.
